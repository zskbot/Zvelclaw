{
  "name": "zvelclaw",
  "version": "0.2.0",
  "description": "AI-native developer CLI for task execution, review gates, GitHub workflows, and deployment.",
  "type": "module",
  "bin": { "zvelclaw": "src/cli.js" },
  "scripts": {
    "start": "node src/cli.js",
    "test": "node --test",
    "lint": "node --check src/cli.js && node --check src/config.js && node --check src/pipeline.js"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
